import React from 'react';
import styles from './Register.module.scss';

import classNames from 'classnames/bind';

const cx = classNames.bind(styles);

function Register({ white = false }) {
    return (
        <>
            
        </>
    );
}

export default Register;
