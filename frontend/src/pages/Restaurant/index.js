import Restaurant from '~/layouts/components/Restaurant';

function RestaurantPage({ children }) {
    return (
        <>
            <Restaurant />
        </>
    );
}

export default RestaurantPage;
